# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-change-this-in-production-12345
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.60

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://192.168.1.38:8081

# Encryption (Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-encryption-key-here

# NFS Configuration
NFS_SERVER=192.168.1.107
NFS_MOUNT_POINT=/mnt/nfs
NFS_SHARE_PATH=/eData
NFS_DATA_PATH=/mnt/nfs/smilelink/data

# HDFS Configuration
HDFS_NAMENODE_URL=http://192.168.1.73:9870
HDFS_USER=hadoop
HDFS_REPLICATION_PATH=/smilelink/data
HDFS_REPLICATION_FACTOR=2

# Google OAuth (Configure later)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Settings
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Storage Settings
USE_NFS=False
USE_HDFS_REPLICATION=False
LOCAL_STORAGE_PATH=./local_data
